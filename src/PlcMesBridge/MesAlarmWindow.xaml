<Window x:Class="PlcMesBridge.MesAlarmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PlcMesBridge.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MesAlarmViewModel, IsDesignTimeCreatable=False}"
        Icon="Assets/app.ico"
        Title="{Binding TitleText, FallbackValue='MES 报警信息'}" Width="800" Height="450" Topmost="True"
        WindowStartupLocation="CenterScreen">
    <!--报警窗（MVVM：展示走 MesAlarmViewModel，本文件纯绑定）。
        设计时 DataContext 为空，展示文本取 FallbackValue 占位（仅预览，运行时走真值）。-->
    <DockPanel Margin="8">
        <Label Content="{Binding MachineName, FallbackValue='机台名称'}" DockPanel.Dock="Top"/>
        <Button Content="{Binding CloseText, FallbackValue='关闭'}" Width="187" Height="41"
                DockPanel.Dock="Bottom" HorizontalAlignment="Center" Margin="0,6,0,0"
                Command="{Binding CloseCommand}"/>
        <TextBox Text="{Binding Message, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" AcceptsReturn="True"/>
    </DockPanel>
</Window>
